package com.springboot;

import java.util.List;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import com.springboot.entity.Board;
import com.springboot.repository.BoardRepository;

@SpringBootTest
public class BoardRepositoryTest {
	
	@Autowired //자동 주입(객체 생성)
	private BoardRepository repository;

	//게시글 추가(등록)
	@Test
	public void insertBoard() {
		/*
		 * Board board = new Board();
		 * 
		 * board.setTitle("가입 인사"); board.setWriter("ミンモ");
		 * board.setContent("こんちゃーよろしくおねあいします");
		 * 
		 * 
		 * repository.save(board); //객체 저장
		 */	
//		Board board = Board.builder()
//				.title("こんにちは")
//				.writer("パチンカス")
//				.content("シンフォギアスマスロうちたいですな")
//				.build();
//		repository.save(board);
	}
	//게시글 목록 보기
	@Test
	public void getBoardLis() {
		List <Board> boardList = repository.findAll();
		
		for(Board board:boardList) {
			System.out.println(board.toString());
		}
	}
}
